# Experiments

The active experiment suite is `revision/`.

Removed legacy IFEval experiment scripts and historical result dumps from this directory. The current revision suite contains the staged K=8 dataset, all automated baselines, LLM-as-Actor baselines, checker logic, scoring, and the unified runner.

Use:

```bash
cd /root/Rethink/experiments/revision
./run_exp.sh
```

